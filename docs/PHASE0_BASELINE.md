# Phase 0 サニタイズ済み基準点

> この文書は2026-07-13時点の変更しない監査記録です。現在の実装・公開可否は[README](../README.md)と[公開前チェック](RELEASE_CHECKLIST.md)を参照してください。

確認日: 2026-07-13 (JST)

## 原本の保全

- 隣接する原本`reitaisai-app.zip`は変更していない。
- サイズは`145,437,153 bytes`で、引継ぎ資料の記録と一致した。
- SHA-256は引継ぎ資料に記録された値と一致した。
- ZIPは20,861エントリで、環境ファイルを除く比較対象39ファイルが作業コピーと一致した。
- 引継ぎ資料には`.wrangler`同梱とあるが、原本ZIP内の該当エントリは0件だった。

原本には秘密情報・実参加者情報・環境固有値が含まれる可能性がある。原本を再配布せず、復元確認が必要な場合だけローカルで参照する。

## Git基準点

原本の未変更状態をそのままコミットすると秘密値を履歴へ残すため、初回コミットは次を除外・サニタイズした基準点とする。

- `node_modules`、生成物、`.wrangler`
- `.env`、`.dev.vars`、実値入り`api/wrangler.toml`
- 実参加者情報を含む可能性があるSQL
- リモートD1実行ログと環境識別値を含む`menu_list.txt`
- ソースへ直書きされていた旧VAPID鍵ペア

旧VAPID秘密鍵は環境バインディング参照へ置き換えた。新しい鍵の生成、Workers Secret登録、既存Push購読の更新は未実施である。

## リモート境界

- リモートD1の参照・変更は行っていない。
- Cloudflare Pages/Workersの公開・変更は行っていない。
- `wrangler deploy`、Pages公開、`--remote`付きD1操作は行っていない。
- 認証方式は未決定で、Phase 1の実装を開始していない。

## 現在の品質状態

- フロントエンドlint: 21 errors / 5 warnings。
- API: 既存依存関係による書込みなしのインメモリバンドルは成功。
- フロントエンドbuild: 既存依存関係で成功。生成物はGit対象外。
- 生成された`dist/sw.js`には`push`、`notificationclick`、`showNotification`がなく、手書きPush処理が本番ビルドで失われる問題を再現した。
- TypeScript専用設定、自動テスト、CIは未整備。
- この基準点は本番運用不可である。

## ロールバック

Phase 0のGit変更は初回コミットを基準に戻せる。サニタイズ前の状態を確認する必要がある場合は、Gitへ取り込まず、ハッシュ検証済み原本ZIPから別の隔離ディレクトリへ展開する。
